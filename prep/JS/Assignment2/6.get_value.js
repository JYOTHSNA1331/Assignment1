const getValue = (a) => {
    if(a=="P" || a=="p")
    return "PrepBytes"
    else if(a=="Z" || a=="z")
    return "Zenith"
    else if(a=="e"||a=="E")
    return "Expert Coder"
    else
    return "Data Structure"
};

